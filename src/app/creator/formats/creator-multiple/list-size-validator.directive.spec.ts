import { ListSizeValidatorDirective } from './list-size-validator.directive';

describe('ListSizeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ListSizeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
